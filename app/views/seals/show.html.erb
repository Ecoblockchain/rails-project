<section class="content-box">
    <p id="notice"><%= notice %></p>
    <% if @view_auth %>
        <p><%= @seal.text %></p>
    <% else %>
        <p>You are not authorized to view this resource.</p>
    <% end %>

    <%= link_to "Back", seals_path%>
</section>
